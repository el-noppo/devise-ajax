#sign_up_modal.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"}
          %i.fa.fa-times-circle
        %h4#SignInModal.modal-title Create an account
        %p Add your details to create an account
      = simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :remote => true, :format => :js,  :html => {:class => "form-signin"}) do |f|
        .modal-body
          //= f.error_notification
          = f.input :email, :autofocus => true, :label => false, :input_html => {:class => 'form-control'}, :placeholder => "E-postadress"
          = f.input :password, :label => false, :input_html => {:class => 'form-control'}, :placeholder => "Lösenord"
          = f.input :password_confirmation, :label => false, :input_html => {:class => 'form-control'}, :placeholder => "Bekräfta lösenord"
          = link_to "Har du redan ett konto?", new_user_session_path
          = link_to "Glömt ditt lösenord?", new_user_password_path
        .modal-footer
          .form-actions
            %a{ :href => "#", :class => "btn btn-sm", "data-dismiss" => "modal" } Avbryt
            = f.button :submit, "Skapa ett konto", :class => "btn btn-sm btn-theme"
